import { SearchAnswerComponent } from './search-answer.component';

describe('SearchAnswerComponent', () => {
  let component: SearchAnswerComponent;

  beforeEach(async () => {
    component = new SearchAnswerComponent();
    // component.ngOnInit();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
